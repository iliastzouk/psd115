/**
 * Extract K2 slide bodies from PDF → src/data/week2/k2PptSlideBodies.generated.js
 * Run: node scripts/build-k2-slides.mjs
 */
import fs from 'fs'
import path from 'path'
import { fileURLToPath } from 'url'
import { PDFParse } from 'pdf-parse'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const root = path.join(__dirname, '..')
const pdfPath = path.join(root, 'public', 'week2', 'k2-psd115.pdf')

const buf = fs.readFileSync(pdfPath)
const parser = new PDFParse({ data: buf })
const { text: raw } = await parser.getText()
await parser.destroy()

const re = /--\s*(\d+)\s+of\s+44\s*--/g
const parts = []
let m
let lastIndex = 0
while ((m = re.exec(raw)) !== null) {
  if (parts.length) {
    parts[parts.length - 1].body = raw.slice(lastIndex, m.index).trim()
  }
  parts.push({ n: parseInt(m[1], 10), body: '' })
  lastIndex = m.index + m[0].length
}
if (parts.length) {
  parts[parts.length - 1].body = raw.slice(lastIndex).trim()
}

const bodies = {}
for (const { n, body } of parts) {
  bodies[n] = body.replace(/\r\n/g, '\n').trim()
}

const keys = Object.keys(bodies)
  .map(Number)
  .sort((a, b) => a - b)
const bodyLines = keys.map((n) => `  ${n}: ${JSON.stringify(bodies[n])},`)
const out = `/* Generated by scripts/build-k2-slides.mjs — do not edit by hand */
export const K2_PPT_TOTAL_SLIDES = 44
/** @type {Record<number, string>} */
export const K2_PPT_SLIDE_BODIES = {
${bodyLines.join('\n')}
}
`
const outPath = path.join(root, 'src', 'data', 'week2', 'k2PptSlideBodies.generated.js')
fs.mkdirSync(path.dirname(outPath), { recursive: true })
fs.writeFileSync(outPath, out, 'utf8')
console.log('Wrote', outPath, keys.length, 'slides')
